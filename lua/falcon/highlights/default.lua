local palette = require('falcon.palette')

return {
  hl_error = palette.red,
  hl_error_dark = palette.mid_red,
  hl_warning = palette.yellow,
  hl_warning_dark = palette.mid_yellow,
  hl_hint = palette.dark_tan,
  hl_info = palette.blue_gray,
  hl_info_dark = palette.mid_gr,
  hl_added_bg = palette.off_green,
  hl_removed_bg = palette.off_red,
  hl_success = palette.green,
  hl_partial = palette.yellow,
  hl_changed = palette.yellow,
  hl_changed_alt = palette.orange,
  hl_deleted = palette.red,
  hl_selection = palette.dark_indigo,
}
