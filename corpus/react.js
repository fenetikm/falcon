export const Dropdown: React.FC<DropdownProps> = ({options, onSelect }) => {
  return (
    <Dropdown>
      <Selection
        data="thing"
        size="blah"
        hasButton
      />
    </Dropdown>
);
}
